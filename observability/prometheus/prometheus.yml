global:
    scrape_interval: 15s
    evaluation_interval: 15s

rule_files:
    - /etc/prometheus/alerts/*.yml

scrape_configs:
    - job_name: "beetrack-api"
      static_configs:
          - targets: ["api:8000"]
      metrics_path: /metrics
    - job_name: "redis"
      static_configs:
          - targets: ["redis_exporter:9121"]
